import { useState } from "react";
import {} from "./config/api"

function App() {
  const [count, setCount] = useState(0);

  return <div>App

    {console.log(instance)}
  </div>;
}

export default App;
